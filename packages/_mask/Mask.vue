<template>
  <transition name="ivc-mask-fade">
    <div class="ivc-mask" v-if="show" @click="handleClick" :style="maskStyle"></div>
  </transition>
</template>
<script>
export default {
  name: 'ivc-mask',
  props: {
    show: {
      type: Boolean,
      default: false
    },
    fixed: {
      type: Boolean,
      default: false
    },
    opacity: {
      type: Number,
      default: 0.4
    },
    color: {
      type: String,
      default: '#000'
    },
    zIndex: {
      type: Number
    },
    onClick: {
      type: Function
    }
  },
  methods: {
    handleClick () {
      if (this.onClick) {
        this.onClick()
      }
    }
  },
  computed: {
    maskStyle () {
      return {
        'opacity': this.opacity,
        'background-color': this.color,
        'position': this.fixed ? 'fixed' : '',
        'z-index': this.zIndex
      }
    }
  }
}
</script>
<style lang="scss">
.ivc-mask {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
}
.ivc-mask-fade-enter-active,
.ivc-mask-fade-leave-active {
  transition: opacity .45s cubic-bezier(0.23, 1, 0.32, 1);
}
.ivc-mask-fade-enter,
.ivc-mask-fade-leave-to {
  opacity: 0 !important;
}
</style>
